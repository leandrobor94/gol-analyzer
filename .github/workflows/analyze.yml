name: Analizar partidos y alertar

on:
  push:
    branches: [main]
  schedule:
    # Cada 10 min, hora Colombia (UTC-5)
    # Mañana/tarde: 12:00-23:59 UTC (7am-6:59pm Colombia)
    - cron: '*/10 12-23 * * *'
    # Noche: 00:00-02:50 UTC (7pm-9:50pm Colombia)
    - cron: '*/10 0-2 * * *'
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Instalar Chromium
        run: npx playwright install chromium --with-deps

      - name: Ejecutar análisis (Flashscore)
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node run_flashscore.js

      - name: Subir screenshot de depuración
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: debug-screenshot
          path: debug_screenshot.png
          if-no-files-found: ignore
